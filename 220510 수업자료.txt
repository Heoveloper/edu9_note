변수 : 값을 저장하기 위해 메모리 공간에 붙여진 이름.

연산식 : 피연산자 연산자 피연산자

지역변수 : 메소드 내에 선언된 변수
		 초기화 이후에 사용할 수 있다.
		 초기화 : 변수가 선언된 이후 최초로 값을 할당하는 표현식

매개변수 : 메소드 외부에서 입력받기 위해 선언된 변수
         메소드 내에서 지역변수처럼 사용됨.
		 
타입: 변수의 값의 종류와 범위(메모리)를 결정함.
     기본형(8개), 참조형 2가지가 존재
	 
연산자 : 산술 > 비교 > 논리 > 대입

실행 클래스 : 메인 메소드(psvm)를 포함하고 있는 클래스.
라이브러리 클래스 : 메인 메소드를 포함하고 있지 않는 클래스.

Ctrl+Shift+Enter

