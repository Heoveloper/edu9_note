인터페이스(inter + face)
─────
메소드 재정의(Overriding)
: 전제조건 1. 메소드 선언부가 동일(반환타입, 메소드명, 매개변수(개수, 타입))
		2. 접근제한자는 동일하거나 가시범위가 넓어야한다.
		3. 인스턴스 메소드만 대상 (private 메소드 제외, final 제외)
		
public > protected > default (접근제한자 생략된 경우) > private
어디든 > 동일패키지+다른패키지(상속관계) > 동일패키지 > 동일클래스